const _ = require("lodash");

module.exports = {
    Query: {
        // allEntries: [Device]!;
        // entries(offSet: Int!, limit: Int!) [Device]!;
        // entry(Id: ID);

        allBookings: (global, args) => {
            return new Promise((resolve, reject) => {

            });
        },

    }
}